spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:none}      # Added 'none'
            client-secret: ${GOOGLE_CLIENT_SECRET:none} # Added 'none'
            scope:
              - email
              - profile
